const CardBody = (props: any) => {
  return (
    <div
      className={`text-center object-cover bg-no-repeat bg-cover bg-gradient-to-r from-[#e5e6e4] to-[#d7d8d4] text-xs`}
      {...props}
    ></div>
  );
};

export default CardBody;
